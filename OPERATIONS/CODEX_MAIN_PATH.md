# CODEX_MAIN_PATH

## Purpose

This file defines the main operating path of the workspace.

At the current phase, the primary AI execution path is:

- Codex as the main engine
- ChatGPT account login as the main authentication path
- workspace as the project root
- Engineering Work Doctrine as the governing method

---

## Current Main Path

### Main Environment
- current personal machine
- workspace root (user-defined location — see Root Model in `AGENTS.md`)
- doctrine at `DocsSB/ENGINEERING_WORK_DOCTRINE` (relative to workspace root)
- active projects at `Projects/_ACTIVE` (relative to workspace root)

### Main AI Path
- Codex
- ChatGPT-based authentication
- doctrine-guided operation

---

## Operational Rule

The main path must be used for:

- discovery
- consolidation
- contract logic
- readiness logic
- structured delivery guidance
- mega-tech protocol activation (files 23-34) for Stable state work

The fallback runtime is not the main operating path.

---

## Current Rule

Gemini runtime remains fallback only.

Codex is the intended main path.

---

## Usage Principle

The workspace should be usable in two ways:

### Comfort Mode
- VS Code
- Codex
- visual workspace operation

### Future Base Mode
- terminal-first
- Codex CLI
- portable workspace operation

At the current stage, Comfort Mode is acceptable.
The workspace must still be designed for future Base Mode.

---

## Final Rule

The main path of the workspace is no longer generic AI usage.

It is:

**Codex operating over the workspace under the Engineering Work Doctrine.**